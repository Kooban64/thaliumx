# This directory contains local secrets for development fallback
# In production, use HashiCorp Vault for secrets management
#
# Files in this directory should be named after the secret key (lowercase, hyphens)
# Example: jwt-secret, db-password, redis-password
#
# DO NOT commit actual secrets to version control