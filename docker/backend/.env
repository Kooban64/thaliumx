# Backend Environment Configuration
NODE_ENV=development
PORT=3002

# Database
DATABASE_URL=postgresql://acme_user:devpassword@localhost:5432/acme

# Redis
REDIS_URL=redis://localhost:6379

# JWT
JWT_SECRET=dev_jwt_secret_key_change_in_production
JWT_EXPIRES_IN=24h

# Production overrides
POSTGRES_PASSWORD=devpassword
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=changeme_in_production
KEYCLOAK_HOSTNAME=localhost

# BlnkFinance
BLNK_FINANCE_URL=http://localhost:5001
BLNK_SECRET_KEY=dev_blnk_secret_key

# Typesense
TYPESENSE_URL=http://localhost:8108
TYPESENSE_API_KEY=dev_typesense_key

# Feature Flags
ENABLE_VAULT=false
ENABLE_KAFKA=false
ENABLE_OTEL=false

# Logging
LOG_LEVEL=debug